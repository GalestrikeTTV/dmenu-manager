#[test]
fn primary_string_test() -> Result<(), std::io::Error> {
    // here we want to make sure that we can send a primary string to dbus
    // and get a good result back

    // We only send ok because I don't have anything setup yet
    Ok(())
}
